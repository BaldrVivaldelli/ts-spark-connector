## API Report File for "ts-spark-connector"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type * as grpc from '@grpc/grpc-js';

// @public
export function abs<T extends NumericValue | null, E>(col: TypedColumn<T, E>): NumericColumn<T, E>;

// @public (undocumented)
export type AddedColumnSchema<S, Name extends string, T extends ColumnType> = [S] extends [UnknownSchema] ? UnknownSchema : S extends SchemaShape ? WithColumn<S, Name, T> : UnknownSchema;

// @public
export type AggFactory<S extends SchemaShape, E> = {
    count(): PendingAggregation<bigint, E>;
    countDistinct(ref: ColRef<S, E>): PendingAggregation<bigint, E>;
    sum<K extends NumericColumnKey<S>>(ref: ColRef<S, E, K>): PendingAggregation<SumResult<S, K> | null, E>;
    avg(ref: ColRef<S, E, NumericColumnKey<S>>): PendingAggregation<number | null, E>;
    min<K extends NumericColumnKey<S>>(ref: ColRef<S, E, K>): PendingAggregation<NumericResult<S, K> | null, E>;
    max<K extends NumericColumnKey<S>>(ref: ColRef<S, E, K>): PendingAggregation<NumericResult<S, K> | null, E>;
};

// @public
export type Aggregated<S extends SchemaShape, K extends keyof S, A extends readonly {
    readonly __out: object;
}[]> = Prettify<Pick<S, K> & UnionToIntersection<A[number]["__out"]>>;

// @public (undocumented)
export type AggregatedSchema<S, K extends PropertyKey, A extends readonly {
    readonly __out: object;
}[]> = S extends SchemaShape ? Aggregated<S, Extract<K, keyof S>, A> : UnknownSchema;

// @public
export type AggregationExprThunk<E> = (EX: ExprAlg<E>) => E;

// @public
export type AmbiguousColumn<Name extends string = string> = {
    readonly __ambiguous: Name;
};

// @public (undocumented)
export interface ArraySpec<Element extends FieldSpec = FieldSpec> {
    // (undocumented)
    readonly element: Element;
    // (undocumented)
    readonly kind: "array";
    // (undocumented)
    readonly nullable?: boolean;
}

// @public (undocumented)
export function arrayType<const Element extends FieldSpec, const N extends boolean = false>(element: Element, nullable?: N): ArraySpec<Element> & NullableFlag<N>;

// @public (undocumented)
export type AuthConfig = {
    type: "basic";
    username: string;
    password: string;
} | {
    type: "token";
    token: string;
};

// @public (undocumented)
export interface BatchFormatCap<W> {
    // (undocumented)
    mode(w: W, m: SaveMode): W;
}

// @public
export type BatchWProgram<R, E, G, CDF = unknown, CEX = unknown> = WProgram<R, E, G, WBatch, CDF, CEX, BatchWriterAlg<R>>;

// @public (undocumented)
export type BatchWriterAlg<R> = WriteCore<R, WBatch> & BatchFormatCap<WBatch> & BucketSortCap<WBatch> & ViewsCap<WBatch>;

// @public (undocumented)
export type BatchWriterFormat = "parquet" | "csv" | "json" | "orc" | "avro";

// @public (undocumented)
export interface BucketSortCap<W> {
    // (undocumented)
    bucketBy(w: W, numBuckets: number, col: string, ...cols: string[]): W;
    // (undocumented)
    sortBy(w: W, col: string, ...cols: string[]): W;
}

// @public
export type BuildableColumn<E> = {
    build(EX: ExprAlg<E>): E;
};

// @public (undocumented)
export interface CacheCap<R> {
    // (undocumented)
    cache(plan: R): R;
    // (undocumented)
    persist(plan: R, level?: string): R;
    // (undocumented)
    unpersist(plan: R, blocking?: boolean): R;
}

// @public (undocumented)
export function call(name: string, args: (EBuilder | string | number | boolean)[]): EBuilder;

// @public (undocumented)
export interface CheckpointCap<W> {
    // (undocumented)
    checkpoint(w: W, location: string): W;
}

// @public (undocumented)
export function coalesce(...xs: Array<EBuilder | string | number | boolean>): EBuilder;

// @public (undocumented)
export type Coalesced<T extends ColumnType, U extends ColumnType> = null extends T ? null extends U ? NonNull<T> | null : NonNull<T> : NonNull<T>;

// @public (undocumented)
export const col: (name: string) => EBuilder;

// @public (undocumented)
export type ColRef<S extends SchemaShape, E, K extends UsableColumnKey<S> = UsableColumnKey<S>> = K | BuildableColumn<E> | ((columns: Columns<S, E>) => BuildableColumn<E>);

// @public (undocumented)
export type ColumnName<S> = [S] extends [UnknownSchema] ? string : S extends SchemaShape ? UsableColumnKey<S> : string;

// @public
export type Columns<S extends SchemaShape, E> = {
    readonly [K in UsableColumnKey<S>]: [NonNull<UsableColumnType<S, K>>] extends [NumericValue] ? NumericColumn<UsableColumnType<S, K> & (NumericValue | null), E> : TypedColumn<UsableColumnType<S, K>, E>;
};

// @public
export type ColumnType = ScalarType | ComplexType | null;

// @public
export type ComplexSpec = DecimalSpec | ArraySpec | MapSpec | StructSpec;

// @public
export type ComplexType = readonly ColumnType[] | ReadonlyMap<ScalarType, ColumnType> | ({
    readonly [field: string]: ColumnType;
} & {
    readonly __ambiguous?: never;
});

// @public
export function concat<E>(...parts: Array<TypedColumn<string | null, E> | string>): TypedColumn<string | null, E>;

// @public
export class Condition<E> {
    // @internal
    constructor(run: TypedColumnExprThunk<E>);
    and(other: Condition<E>): Condition<E>;
    build(EX: ExprAlg<E>): E;
    or(other: Condition<E>): Condition<E>;
}

// @public (undocumented)
export function createSparkSession(sessionId?: string): SparkSession;

// @public (undocumented)
export type DataFrameExpressionBuilder = {
    build<E>(EX: ExprAlg<E>): E;
};

// @public (undocumented)
export class DataFrameReaderTF<R = unknown, E = unknown, G = unknown> {
    constructor(session: SparkSession, fmt?: string, opts?: ReaderOptions);
    // (undocumented)
    avro(...paths: string[]): ReadChainedDataFrame<UnknownSchema, R, E, G>;
    // (undocumented)
    csv(...paths: string[]): ReadChainedDataFrame<UnknownSchema, R, E, G>;
    // (undocumented)
    format(fmt: string): DataFrameReaderTF<R, E, G>;
    // (undocumented)
    json(...paths: string[]): ReadChainedDataFrame<UnknownSchema, R, E, G>;
    // (undocumented)
    load(...paths: string[]): ReadChainedDataFrame<UnknownSchema, R, E, G>;
    // (undocumented)
    option(key: string, value: ReaderOptionValue): DataFrameReaderTF<R, E, G>;
    // (undocumented)
    options(kv: Record<string, ReaderOptionValue>): DataFrameReaderTF<R, E, G>;
    // (undocumented)
    orc(...paths: string[]): ReadChainedDataFrame<UnknownSchema, R, E, G>;
    // (undocumented)
    parquet(...paths: string[]): ReadChainedDataFrame<UnknownSchema, R, E, G>;
    readWith<D extends SchemaDef>(declared: DeclaredSchema<D>, format: string, path: string | string[], options?: Record<string, ReaderOptionValue>): ReadChainedDataFrame<InferSchema<D>, R, E, G>;
    // (undocumented)
    sql(query: string): ReadChainedDataFrame<UnknownSchema, R, E, G, SqlCap<R>, unknown>;
    // (undocumented)
    table(name: string): ReadChainedDataFrame<UnknownSchema, R, E, G, SqlCap<R>, unknown>;
    // (undocumented)
    text(...paths: string[]): ReadChainedDataFrame<UnknownSchema, R, E, G>;
}

// @public (undocumented)
export class DataFrameWriterTF<R = DefaultDataFrameRepresentation, E = unknown, G = unknown, W = unknown, CDF = unknown, CEX = unknown, WRALG = unknown> {
    // (undocumented)
    avro(this: DataFrameWriterTF<R, E, G, WBatch, CDF, CEX, WRALG>): DataFrameWriterTF<R, E, G, WBatchBrand, CDF, CEX, WRALG>;
    // (undocumented)
    awaitTermination(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>): DataFrameWriterTF<R, E, G, WStreamBrand, CDF, CEX, WRALG>;
    // (undocumented)
    bucketBy(this: DataFrameWriterTF<R, E, G, WBatch, CDF, CEX, WRALG>, n: number, col: string, ...cols: string[]): DataFrameWriterTF<R, E, G, WBatchBrand, CDF, CEX, WRALG>;
    // (undocumented)
    checkpoint(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>, path: string): DataFrameWriterTF<R, E, G, WStreamBrand, CDF, CEX, WRALG>;
    // (undocumented)
    console(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>): DataFrameWriterTF<R, E, G, WStreamBrand, CDF, CEX, WRALG>;
    // (undocumented)
    createOrReplaceTempView(viewName: string): Promise<void>;
    // (undocumented)
    createOrReplaceTempView(viewName: string, impl: WriterBackendImplementation<R, E, G, W, CDF, CEX, WRALG>): Promise<void>;
    // (undocumented)
    createTempView(viewName: string): Promise<void>;
    // (undocumented)
    createTempView(viewName: string, impl: WriterBackendImplementation<R, E, G, W, CDF, CEX, WRALG>): Promise<void>;
    // (undocumented)
    csv(this: DataFrameWriterTF<R, E, G, WBatch, CDF, CEX, WRALG>): DataFrameWriterTF<R, E, G, WBatchBrand, CDF, CEX, WRALG>;
    // (undocumented)
    format(this: DataFrameWriterTF<R, E, G, W, CDF, CEX, WRALG>, fmt: string): DataFrameWriterTF<R, E, G, W, CDF, CEX, WRALG>;
    // (undocumented)
    static fromBatchProgram<R, E, G, CDF, CEX>(df: ReadDFAny<R, E, G, CDF, CEX>, prog: BatchWProgram<R, E, G, CDF, CEX>): DataFrameWriterTF<R, E, G, WBatch, CDF, CEX, BatchWriterAlg<R>>;
    // (undocumented)
    static fromParts<R, E, G, W, CDF, CEX, WRALG>(args: {
        session: SparkSession;
        dfProgram: DFProgram<R, E, G, CDF, CEX>;
        wProgram: WProgram<R, E, G, W, CDF, CEX, WRALG>;
        WR?: WRALG;
        EXE?: DFWritingExec<W>;
        DF?: DFAlg<R, E, G, CDF>;
        EX?: ExprAlg<E> & CEX;
    }): DataFrameWriterTF<R, E, G, W, CDF, CEX, WRALG>;
    // (undocumented)
    static fromStreamProgram<R, E, G, CDF, CEX>(df: ReadDFAny<R, E, G, CDF, CEX>, prog: StreamWProgram<R, E, G, CDF, CEX>): DataFrameWriterTF<R, E, G, WStream, CDF, CEX, StreamWriterAlg<R>>;
    // (undocumented)
    fromTempView(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>, name: string): DataFrameWriterTF<R, E, G, WStreamBrand, CDF, CEX, WRALG>;
    // (undocumented)
    json(this: DataFrameWriterTF<R, E, G, WBatch, CDF, CEX, WRALG>): DataFrameWriterTF<R, E, G, WBatchBrand, CDF, CEX, WRALG>;
    // (undocumented)
    kafka(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>): DataFrameWriterTF<R, E, G, WStreamBrand, CDF, CEX, WRALG>;
    // (undocumented)
    memory(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>): DataFrameWriterTF<R, E, G, WStreamBrand, CDF, CEX, WRALG>;
    // (undocumented)
    mode(this: DataFrameWriterTF<R, E, G, WBatch, CDF, CEX, WRALG>, m: SaveMode): DataFrameWriterTF<R, E, G, WBatchBrand, CDF, CEX, WRALG>;
    // (undocumented)
    option(k: string, v: WriterOptionValue): DataFrameWriterTF<R, E, G, W, CDF, CEX, WRALG>;
    // (undocumented)
    options(opts: Record<string, WriterOptionValue>): DataFrameWriterTF<R, E, G, W, CDF, CEX, WRALG>;
    // (undocumented)
    orc(this: DataFrameWriterTF<R, E, G, WBatch, CDF, CEX, WRALG>): DataFrameWriterTF<R, E, G, WBatchBrand, CDF, CEX, WRALG>;
    // (undocumented)
    outputMode(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>, m: "append" | "complete" | "update"): DataFrameWriterTF<R, E, G, WStreamBrand, CDF, CEX, WRALG>;
    // (undocumented)
    parquet(this: DataFrameWriterTF<R, E, G, WBatch, CDF, CEX, WRALG>): DataFrameWriterTF<R, E, G, WBatchBrand, CDF, CEX, WRALG>;
    // (undocumented)
    partitionBy(...cols: string[]): DataFrameWriterTF<R, E, G, W, CDF, CEX, WRALG>;
    // (undocumented)
    queryName(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>, name: string): DataFrameWriterTF<R, E, G, WStreamBrand, CDF, CEX, WRALG>;
    // (undocumented)
    save(): Promise<void>;
    // (undocumented)
    save(path: string): Promise<void>;
    // (undocumented)
    save(impl: WriterBackendImplementation<R, E, G, W, CDF, CEX, WRALG>): Promise<void>;
    // (undocumented)
    save(path: string, impl: WriterBackendImplementation<R, E, G, W, CDF, CEX, WRALG>): Promise<void>;
    // (undocumented)
    saveAsTable(table: string): Promise<void>;
    // (undocumented)
    saveAsTable(table: string, impl: WriterBackendImplementation<R, E, G, W, CDF, CEX, WRALG>): Promise<void>;
    // (undocumented)
    sortBy(this: DataFrameWriterTF<R, E, G, WBatch, CDF, CEX, WRALG>, col: string, ...cols: string[]): DataFrameWriterTF<R, E, G, WBatchBrand, CDF, CEX, WRALG>;
    // (undocumented)
    start(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>, path?: string): Promise<StreamingQueryHandle>;
    // (undocumented)
    startAndAwaitTermination(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>, path?: string): Promise<StreamingQueryHandle>;
    // (undocumented)
    text(this: DataFrameWriterTF<R, E, G, WBatch, CDF, CEX, WRALG>): DataFrameWriterTF<R, E, G, WBatchBrand, CDF, CEX, WRALG>;
    // (undocumented)
    toClientASTJSON(): string;
    // (undocumented)
    toClientASTMermaid(): string;
    // (undocumented)
    toTable(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>, table: string): Promise<StreamingQueryHandle>;
    // (undocumented)
    trigger(this: DataFrameWriterTF<R, E, G, WStream, CDF, CEX, WRALG>, t: StreamTriggerInput): DataFrameWriterTF<R, E, G, WStreamBrand, CDF, CEX, WRALG>;
    // (undocumented)
    withBackend(impl: WriterBackendImplementation<R, E, G, W, CDF, CEX, WRALG>): DataFrameWriterTF<R, E, G, W, CDF, CEX, WRALG>;
}

// @public
export interface DecimalSpec {
    // (undocumented)
    readonly kind: "decimal";
    // (undocumented)
    readonly nullable?: boolean;
    // (undocumented)
    readonly precision: number;
    // (undocumented)
    readonly scale: number;
}

// @public (undocumented)
export function decimalType<const P extends number, const S extends number, const N extends boolean = false>(precision: P, scale: S, nullable?: N): DecimalSpec & {
    readonly precision: P;
    readonly scale: S;
} & NullableFlag<N>;

// @public
export class DeclaredSchema<D extends SchemaDef> {
    // @internal
    constructor(def: D);
    // @internal
    readonly __schema: InferSchema<D>;
    // (undocumented)
    readonly def: D;
    toDDL(): string;
}

// @public (undocumented)
export type DefaultDataFrameRepresentation = unknown;

// @public (undocumented)
export interface DescribeCap<R, E> {
    // (undocumented)
    describe(plan: R, columns: E[]): R;
}

// @public (undocumented)
export type DFAlg<R, E, G = unknown, ExtraCaps = {}> = DFBatchCaps<R, E, G> & ExtraCaps;

// @public (undocumented)
export type DFBatchCaps<R, E, G> = DFCore<R, E, G> & CacheCap<R> & RepartitionCap<R> & SqlCap<R> & HintCap<R> & SamplingCap<R> & DescribeCap<R, E> & SummaryCap<R, E>;

// @public (undocumented)
export interface DFCore<R, E, G = unknown> {
    // (undocumented)
    agg(group: G, aggregations: Record<string, E>, groupType?: GroupTypeInput): R;
    // (undocumented)
    distinct(plan: R): R;
    // (undocumented)
    drop(plan: R, columnNames: string[]): R;
    // (undocumented)
    dropDuplicates(plan: R, cols?: E[]): R;
    // (undocumented)
    except(left: R, right: R, opts?: {
        all?: boolean;
    }): R;
    // (undocumented)
    filter(plan: R, condition: E): R;
    // (undocumented)
    groupBy(plan: R, cols: E[]): G;
    // (undocumented)
    intersect(left: R, right: R, opts?: {
        all?: boolean;
    }): R;
    // (undocumented)
    join(left: R, right: R, on: E, joinType?: JoinTypeInput): R;
    // (undocumented)
    limit(plan: R, n: number): R;
    // (undocumented)
    orderBy(plan: R, orders: SortOrder<E>[]): R;
    // (undocumented)
    relation(format: string, path: string | string[], options?: Record<string, string>, schema?: string): R;
    // (undocumented)
    select(plan: R, columns: E[]): R;
    // (undocumented)
    sort(plan: R, orders: SortOrder<E>[]): R;
    // (undocumented)
    union(left: R, right: R, opts?: {
        byName?: boolean;
        allowMissingColumns?: boolean;
    }): R;
    // (undocumented)
    withColumn(plan: R, name: string, column: E): R;
    // (undocumented)
    withColumnRenamed(plan: R, oldName: string, newName: string): R;
    // (undocumented)
    withColumnsRenamed(plan: R, mapping: Record<string, string>): R;
    withPlanId?(plan: R, planId: number): R;
}

// @public (undocumented)
export type DFProgram<R = unknown, E = unknown, G = unknown, CDF = unknown, CEX = unknown> = (DF: DFAlg<R, E, G, CDF>, EX: ExprAlg<E> & CEX) => R;

// @public (undocumented)
export interface DFWritingExec<W> {
    // (undocumented)
    run(root: W, session: SparkSession): Promise<void>;
    // (undocumented)
    runStream(root: W, session: SparkSession): Promise<StreamingQueryHandle>;
}

// @public
export type Dropped<S extends SchemaShape, K extends keyof S> = Prettify<Omit<S, K>>;

// @public (undocumented)
export type DroppedSchema<S, K extends PropertyKey> = [S] extends [UnknownSchema] ? UnknownSchema : S extends SchemaShape ? Dropped<S, Extract<K, keyof S>> : UnknownSchema;

// @public (undocumented)
export type DuplicateRenameTargets<M extends Record<string, string>> = {
    [K in keyof M]: {
        [P in Exclude<keyof M, K>]: M[P] extends M[K] ? M[K] extends M[P] ? M[K] : never : never;
    }[Exclude<keyof M, K>];
}[keyof M];

// @public (undocumented)
export type EBuilder = {
    build<E>(EX: ExprAlg<E>): E;
    alias(name: string): EBuilder;
    eq(x: EBuilder | string | number | boolean): EBuilder;
    gt(x: EBuilder | number): EBuilder;
    gte(x: EBuilder | number): EBuilder;
    lt(x: EBuilder | number): EBuilder;
    lte(x: EBuilder | number): EBuilder;
    and(x: EBuilder): EBuilder;
    or(x: EBuilder): EBuilder;
    asc(nulls?: NullsOrder): SortKeyBuilder;
    desc(nulls?: NullsOrder): SortKeyBuilder;
    ascNullsFirst(): SortKeyBuilder;
    ascNullsLast(): SortKeyBuilder;
    descNullsFirst(): SortKeyBuilder;
    descNullsLast(): SortKeyBuilder;
    isNull(): EBuilder;
    isNotNull(): EBuilder;
    over(spec: (EX: ExprAlg<any>) => WindowSpec<any>): EBuilder;
    from_json(schema: string): EBuilder;
    to_json(): EBuilder;
};

// @public (undocumented)
export const elementAt: (mapCol: EBuilder, key: EBuilder | string | number) => EBuilder;

// @public (undocumented)
export interface EventTimeWatermarkCap<R, E> {
    // (undocumented)
    withWatermark(plan: R, eventTimeCol: E, delay: string): R;
}

// @public (undocumented)
export type ExplainModeInput = "simple" | "extended" | "codegen" | "cost" | "formatted";

// @public (undocumented)
export function explode(col: EBuilder | string): EBuilder;

// @public (undocumented)
export interface ExprAlg<E> {
    // (undocumented)
    alias(input: E, ...names: string[]): E;
    // (undocumented)
    bin(op: string, left: E, right: E): E;
    // (undocumented)
    call(name: string, args: E[]): E;
    // (undocumented)
    caseWhen(branches: Array<{
        when: E;
        then: E;
    }>, elze?: E): E;
    // (undocumented)
    coalesce(args: E[]): E;
    // (undocumented)
    col(name: string, planId?: number): E;
    // (undocumented)
    elementAt(map: E, key: E): E;
    // (undocumented)
    explode(input: E): E;
    // (undocumented)
    from_json(jsonExpr: E, schema: string): E;
    // (undocumented)
    getField(input: E, field: string): E;
    // (undocumented)
    getItem(collectionExpr: E, key: E | string | number): E;
    // (undocumented)
    isNotNull(input: E): E;
    // (undocumented)
    isNull(input: E): E;
    // (undocumented)
    lit(v: LiteralValue): E;
    // (undocumented)
    logical(op: "AND" | "OR", left: E, right: E): E;
    // (undocumented)
    map_keys(input: E): E;
    // (undocumented)
    map_values(input: E): E;
    // (undocumented)
    posexplode(input: E): E;
    // (undocumented)
    sortKey(input: E, direction: SortDirection, nulls?: NullsOrder): E;
    // (undocumented)
    split(input: E, delimiter: E | string): E;
    // (undocumented)
    star(): E;
    // (undocumented)
    to_json(expr: E): E;
    // (undocumented)
    win(func: E, spec: WindowSpec<E>): E;
}

// @public (undocumented)
export type ExpressionCaseChain = {
    when(cond: EBuilder, val: EBuilder | string | number | boolean): ExpressionCaseChain;
    otherwise(val: EBuilder | string | number | boolean): EBuilder;
};

// @public
export type FieldSpec = TypeToken | NullableToken | ComplexSpec;

// @public
export type FieldToken = TypeToken | NullableToken;

// @public (undocumented)
export type FrameBoundary = {
    type: "UnboundedPreceding";
} | {
    type: "UnboundedFollowing";
} | {
    type: "CurrentRow";
} | {
    type: "ValuePreceding";
    value: number;
} | {
    type: "ValueFollowing";
    value: number;
};

// @public (undocumented)
export type FrameType = "rows" | "range";

// @public (undocumented)
export function from_json(jsonExpr: EBuilder, schema: string): EBuilder;

// @public (undocumented)
export function getField(structCol: EBuilder, field: string): EBuilder;

// @public (undocumented)
export function getItem(collection: EBuilder, key: EBuilder | string | number): EBuilder;

// @public
export class GroupedDataFrameTF<S, K extends PropertyKey, R, E, G, CDF, CEX> {
    constructor(source: ReadChainedDataFrame<S, R, E, G, CDF, CEX>, grouping: ReadonlyArray<string | DataFrameExpressionBuilder>);
    // (undocumented)
    agg<const A extends readonly TypedAggregation<string, ColumnType, E>[]>(build: TypedAggBuilder<S, E, A>): ReadChainedDataFrame<AggregatedSchema<S, K, A>, R, E, G, CDF, CEX>;
    // (undocumented)
    agg(aggregations: Record<string, DataFrameExpressionBuilder | string>): ReadChainedDataFrame<UnknownSchema, R, E, G, CDF, CEX>;
}

// @public (undocumented)
export type GroupTypeInput = "groupby" | "rollup" | "cube" | "pivot";

// @public (undocumented)
export interface HintCap<R> {
    // (undocumented)
    hint(plan: R, name: JoinHintName | string, params?: unknown[]): R;
}

// @public (undocumented)
export type InferField<F extends FieldSpec> = F extends `${TypeToken}?` ? InferNonNullable<F> | null : F extends {
    readonly nullable: true;
} ? InferNonNullable<F> | null : InferNonNullable<F>;

// @public
export type InferNestedSchema<D extends SchemaDef> = {
    [K in keyof D]: InferField<D[K]> | null;
};

// @public
export type InferNonNullable<F extends FieldSpec> = F extends `${infer B}?` ? B extends TypeToken ? TokenToTs[B] : never : F extends TypeToken ? TokenToTs[F] : F extends DecimalSpec ? string : F extends ArraySpec<infer Element> ? Array<InferField<Element> | null> : F extends MapSpec<infer Key, infer Value> ? Map<Extract<InferNonNullable<Key>, string | number | bigint | boolean>, InferField<Value> | null> : F extends StructSpec<infer Fields> ? InferNestedSchema<Fields> : never;

// @public
export type InferSchema<D extends SchemaDef> = {
    [K in keyof D]: InferField<D[K]>;
};

// @public
export type IsKnownSchema<S> = [S] extends [UnknownSchema] ? false : S extends Schema ? true : false;

// @public (undocumented)
export type IsLeftNullable<J extends string> = JoinName<J> extends "RIGHT" | "RIGHT_OUTER" | "OUTER" | "FULL" | "FULL_OUTER" ? true : false;

// @public (undocumented)
export function isNotNull(x: EBuilder | string): EBuilder;

// @public (undocumented)
export function isNull(x: EBuilder | string): EBuilder;

// @public (undocumented)
export type IsRightNullable<J extends string> = JoinName<J> extends "LEFT" | "LEFT_OUTER" | "OUTER" | "FULL" | "FULL_OUTER" ? true : false;

// @public
export type Joined<L extends SchemaShape, R extends SchemaShape> = JoinedFor<L, R, "INNER">;

// @public
export type JoinedFor<L extends SchemaShape, R extends SchemaShape, J extends string> = J extends unknown ? JoinedForSingle<L, R, J> : never;

// @public
export type JoinedForSingle<L extends SchemaShape, R extends SchemaShape, J extends string> = JoinName<J> extends "LEFT_SEMI" | "LEFT_ANTI" ? Prettify<L> : Prettify<{
    [K in Exclude<keyof L, keyof R>]: IsLeftNullable<J> extends true ? NullableColumn<L[K]> : L[K];
} & {
    [K in Exclude<keyof R, keyof L>]: IsRightNullable<J> extends true ? NullableColumn<R[K]> : R[K];
} & {
    [K in keyof L & keyof R]: AmbiguousColumn<K & string>;
}>;

// @public (undocumented)
export type JoinHintName = "broadcast" | "merge" | "shuffle_hash" | "shuffle_replicate_nl";

// @public (undocumented)
export type JoinName<J extends string> = Uppercase<J>;

// @public (undocumented)
export type JoinSchema<S, RS extends SchemaShape, JT extends JoinTypeInput> = S extends SchemaShape ? JoinedFor<S, RS, JT> : UnknownSchema;

// @public (undocumented)
export type JoinTypeInput = JoinTypeName | Lowercase<JoinTypeName>;

// @public (undocumented)
export type JoinTypeName = "INNER" | "LEFT" | "RIGHT" | "OUTER" | "FULL" | "LEFT_OUTER" | "RIGHT_OUTER" | "FULL_OUTER" | "LEFT_SEMI" | "LEFT_ANTI" | "CROSS";

// @public
function length_2<E>(col: TypedColumn<string | null, E>): NumericColumn<number | null, E>;
export { length_2 as length }

// @public (undocumented)
export const lit: (v: LiteralValue) => EBuilder;

// @public (undocumented)
export type LiteralValue = string | number | boolean | bigint | null;

// @public
export function lower<T extends string | null, E>(col: TypedColumn<T, E>): TypedColumn<T, E>;

// @public
export function makeAggFactory<S extends SchemaShape, E>(): AggFactory<S, E>;

// @public (undocumented)
export const map_keys: (mapCol: EBuilder) => EBuilder;

// @public (undocumented)
export const map_values: (mapCol: EBuilder) => EBuilder;

// @public (undocumented)
export interface MapSpec<Key extends FieldSpec = FieldSpec, Value extends FieldSpec = FieldSpec> {
    // (undocumented)
    readonly key: Key;
    // (undocumented)
    readonly kind: "map";
    // (undocumented)
    readonly nullable?: boolean;
    // (undocumented)
    readonly value: Value;
}

// @public (undocumented)
export function mapType<const Key extends FieldSpec, const Value extends FieldSpec, const N extends boolean = false>(key: Key, value: Value, nullable?: N): MapSpec<Key, Value> & NullableFlag<N>;

// @public
export type NonNull<T> = T extends null ? never : T;

// @public
export type NullableColumn<T> = T extends ColumnType ? T | null : T;

// @public
export type NullableFlag<N extends boolean> = N extends true ? {
    readonly nullable: true;
} : object;

// @public
export type NullableToken = `${TypeToken}?`;

// @public (undocumented)
export type NullsOrder = "nullsFirst" | "nullsLast";

// @public (undocumented)
export type NumericBase<T> = Extract<NonNull<T>, NumericValue>;

// @public
export class NumericColumn<T extends NumericValue | null, E> extends TypedColumn<T, E> {
    div(other: NumericOperand<E>): NumericColumn<number | null, E>;
    minus<O extends NumericOperand<E>>(other: O): NumericColumn<PromotedNumeric<T, O> | null, E>;
    plus<O extends NumericOperand<E>>(other: O): NumericColumn<PromotedNumeric<T, O> | null, E>;
    times<O extends NumericOperand<E>>(other: O): NumericColumn<PromotedNumeric<T, O> | null, E>;
}

// @public (undocumented)
export type NumericColumnKey<S extends SchemaShape> = {
    [K in UsableColumnKey<S>]: NonNull<UsableColumnType<S, K>> extends NumericValue ? K : never;
}[UsableColumnKey<S>];

// @public (undocumented)
export type NumericOperand<E> = NumericValue | TypedColumn<NumericValue | null, E>;

// @public (undocumented)
export type NumericOperandValue<O> = O extends TypedColumn<infer V, infer _E> ? Extract<NonNull<V>, NumericValue> : O extends bigint ? bigint : O extends number ? number extends O ? number | bigint : `${O}` extends `${bigint}` ? bigint : number : never;

// @public (undocumented)
export type NumericResult<S extends SchemaShape, K extends NumericColumnKey<S>> = Extract<NonNull<UsableColumnType<S, K>>, NumericValue>;

// @public (undocumented)
export type NumericValue = number | bigint;

// @public (undocumented)
export type OutputMode = "append" | "update" | "complete";

// @public (undocumented)
export interface OutputModeCap<W> {
    // (undocumented)
    outputMode(w: W, m: OutputMode): W;
}

// @public
export class PendingAggregation<T extends ColumnType, E> {
    // @internal
    constructor(run: AggregationExprThunk<E>);
    as<const Alias extends string>(alias: Alias extends "" ? never : Alias): TypedAggregation<Alias, T, E>;
}

// @public (undocumented)
export function posexplode(col: EBuilder | string): EBuilder;

// @public
export type Prettify<T> = {
    [K in keyof T]: T[K];
} & {};

// @public (undocumented)
export type PromotedNumeric<L, O> = number extends NumericBase<L> ? number : NumericOperandValue<O> extends bigint ? bigint : NumericOperandValue<O> extends number ? number : number | bigint;

// @public (undocumented)
export interface QueryNameCap<W> {
    // (undocumented)
    queryName(w: W, name: string): W;
}

// @public
export class ReadChainedDataFrame<S = UnknownSchema, R = unknown, E = unknown, G = unknown, CDF = {}, CEX = {}> {
    constructor(p: DFProgram<R, E, G, CDF, CEX>, session: SparkSession, streaming?: boolean, runtimeSchema?: SchemaDef | undefined);
    as<T extends Schema>(): ReadChainedDataFrame<T, R, E, G, CDF, CEX>;
    // (undocumented)
    broadcast(): ReadChainedDataFrame<S, R, E, G, CDF & HintCap<R>, CEX>;
    // (undocumented)
    cache(): ReadChainedDataFrame<S, R, E, G, CDF & CacheCap<R>, CEX>;
    // (undocumented)
    coalesce(numPartitions: number): ReadChainedDataFrame<S, R, E, G, CDF & RepartitionCap<R>, CEX>;
    // (undocumented)
    coalesce(name: string, ...exprs: Array<string | DataFrameExpressionBuilder | number | boolean>): ReadChainedDataFrame<UnknownSchema, R, E, G, CDF, CEX>;
    // (undocumented)
    coalescePartitions(numPartitions: number): ReadChainedDataFrame<S, R, E, G, CDF & RepartitionCap<R>, CEX>;
    collect(): Promise<Array<RowOf<S>>>;
    // (undocumented)
    collectRaw(): Promise<unknown[]>;
    // (undocumented)
    describe<K extends ColumnName<S>>(colNames: readonly K[]): ReadChainedDataFrame<StatisticsSchema<S, K>, R, E, G, CDF, CEX>;
    // (undocumented)
    distinct(): ReadChainedDataFrame<S, R, E, G, CDF, CEX>;
    // (undocumented)
    drop<K extends ColumnName<S>>(...columnNames: K[]): ReadChainedDataFrame<DroppedSchema<S, K>, R, E, G, CDF, CEX>;
    // (undocumented)
    dropDuplicates<K extends ColumnName<S>>(...cols: K[]): ReadChainedDataFrame<S, R, E, G, CDF, CEX>;
    // (undocumented)
    dropDuplicates(...cols: DataFrameExpressionBuilder[]): ReadChainedDataFrame<S, R, E, G, CDF, CEX>;
    // (undocumented)
    except<RCDF = unknown, RCEX = unknown>(right: ReadChainedDataFrame<S, R, E, G, RCDF, RCEX>): ReadChainedDataFrame<S, R, E, G, CDF & RCDF, CEX & RCEX>;
    // (undocumented)
    exceptAll<RCDF = unknown, RCEX = unknown>(right: ReadChainedDataFrame<S, R, E, G, RCDF, RCEX>): ReadChainedDataFrame<S, R, E, G, CDF & RCDF, CEX & RCEX>;
    // (undocumented)
    explain(mode?: ExplainModeInput): Promise<string>;
    // (undocumented)
    filter(predicate: TypedPredicate<S, E>): ReadChainedDataFrame<S, R, E, G, CDF, CEX>;
    // (undocumented)
    filter(cond: DataFrameExpressionBuilder): ReadChainedDataFrame<S, R, E, G, CDF, CEX>;
    // (undocumented)
    static fromCSV<R, E, G, CDF = {}, CEX = {}>(path: string | string[], session: SparkSession, options?: Record<string, string>): ReadChainedDataFrame<UnknownSchema, R, E, G, CDF, CEX>;
    // (undocumented)
    getProgram(): DFProgram<R, E, G, CDF, CEX>;
    // (undocumented)
    getSession(): SparkSession;
    // (undocumented)
    groupBy<K extends ColumnName<S>>(...by: K[]): GroupedDataFrameTF<S, K, R, E, G, CDF, CEX>;
    // (undocumented)
    groupBy(...by: DataFrameExpressionBuilder[]): GroupedDataFrameTF<UnknownSchema, string, R, E, G, CDF, CEX>;
    // (undocumented)
    hint(name: JoinHintName | string, ...params: any[]): ReadChainedDataFrame<S, R, E, G, CDF & HintCap<R>, CEX>;
    // (undocumented)
    intersect<RCDF = unknown, RCEX = unknown>(right: ReadChainedDataFrame<S, R, E, G, RCDF, RCEX>): ReadChainedDataFrame<S, R, E, G, CDF & RCDF, CEX & RCEX>;
    // (undocumented)
    intersectAll<RCDF = unknown, RCEX = unknown>(right: ReadChainedDataFrame<S, R, E, G, RCDF, RCEX>): ReadChainedDataFrame<S, R, E, G, CDF & RCDF, CEX & RCEX>;
    // @internal
    isStreamingDataFrame(): boolean;
    // (undocumented)
    join<RS extends SchemaShape, JT extends JoinTypeInput = "INNER", RCDF = unknown, RCEX = unknown>(right: ReadChainedDataFrame<RS, R, E, G, RCDF, RCEX>, on: TypedJoinPredicate<S, RS, E>, jt?: JT): ReadChainedDataFrame<JoinSchema<S, RS, JT>, R, E, G, CDF & RCDF, CEX & RCEX>;
    // (undocumented)
    join<RS = unknown, RCDF = unknown, RCEX = unknown>(right: ReadChainedDataFrame<RS, R, E, G, RCDF, RCEX>, on: DataFrameExpressionBuilder, jt?: JoinTypeInput): ReadChainedDataFrame<UnknownSchema, R, E, G, CDF & RCDF, CEX & RCEX>;
    // (undocumented)
    limit(n: number): ReadChainedDataFrame<S, R, E, G, CDF, CEX>;
    // (undocumented)
    mergeHint(): ReadChainedDataFrame<S, R, E, G, CDF & HintCap<R>, CEX>;
    // (undocumented)
    orderBy(...colsOrKeys: Array<ColumnName<S> | DataFrameExpressionBuilder | TypedOrderFactory<S, E>>): ReadChainedDataFrame<S, R, E, G, CDF, CEX>;
    // (undocumented)
    orderBy(...colsOrKeys: Array<ColumnName<S> | DataFrameExpressionBuilder | ((EX: ExprAlg<any>) => SortOrder<any>)>): ReadChainedDataFrame<S, R, E, G, CDF, CEX>;
    // (undocumented)
    persist(level?: string): ReadChainedDataFrame<S, R, E, G, CDF & CacheCap<R>, CEX>;
    // (undocumented)
    randomSplit(weights: number[], seed?: number): ReadChainedDataFrame<S, R, E, G, CDF & SamplingCap<R>, CEX>[];
    // (undocumented)
    static readStream<R, E, G, CDF = unknown, CEX = unknown>(format: string, session: SparkSession, options?: Record<string, string>): ReadChainedDataFrame<UnknownSchema, R, E, G, CDF & StreamingReadCap<R> & StreamingMark<R>, CEX>;
    // (undocumented)
    repartition(numPartitions: number, shuffle?: boolean): ReadChainedDataFrame<S, R, E, G, CDF & RepartitionCap<R>, CEX>;
    // (undocumented)
    runWith(DF: DFAlg<R, E, G, CDF>, EX: ExprAlg<E> & CEX): R;
    // (undocumented)
    sample(fraction: number, withReplacement?: boolean, seed?: number, deterministicOrder?: boolean): ReadChainedDataFrame<S, R, E, G, CDF & SamplingCap<R>, CEX>;
    // (undocumented)
    select<K extends ColumnName<S>>(...cols: K[]): ReadChainedDataFrame<SelectedSchema<S, K>, R, E, G, CDF, CEX>;
    // (undocumented)
    select(...cols: DataFrameExpressionBuilder[]): ReadChainedDataFrame<UnknownSchema, R, E, G, CDF, CEX>;
    show(n?: number, truncate?: number): Promise<void>;
    // (undocumented)
    shuffleHashHint(): ReadChainedDataFrame<S, R, E, G, CDF & HintCap<R>, CEX>;
    // (undocumented)
    shuffleReplicateNLHint(): ReadChainedDataFrame<S, R, E, G, CDF & HintCap<R>, CEX>;
    // (undocumented)
    sort(...colsOrKeys: Array<ColumnName<S> | DataFrameExpressionBuilder | TypedOrderFactory<S, E>>): ReadChainedDataFrame<S, R, E, G, CDF, CEX>;
    // (undocumented)
    sort(...colsOrKeys: Array<ColumnName<S> | DataFrameExpressionBuilder | ((EX: ExprAlg<any>) => SortOrder<any>)>): ReadChainedDataFrame<S, R, E, G, CDF, CEX>;
    // @deprecated (undocumented)
    sql(query: string): ReadChainedDataFrame<UnknownSchema, R, E, G, CDF & SqlCap<R>, CEX>;
    streamRaw(): AsyncGenerator<Record<string, unknown>, void, void>;
    // (undocumented)
    summary<K extends ColumnName<S>>(metrics: readonly string[] | undefined, colNames: readonly K[]): ReadChainedDataFrame<StatisticsSchema<S, K>, R, E, G, CDF, CEX>;
    toArrowBatches(): AsyncGenerator<Buffer, void, void>;
    // (undocumented)
    toClientASTJSON(): string;
    // (undocumented)
    toClientASTMermaid(): string;
    // (undocumented)
    toProtoJSON(): string;
    toRows(): AsyncGenerator<RowOf<S>, void, void>;
    // (undocumented)
    toSparkLogicalPlanJSON(): string;
    // (undocumented)
    union(right: ReadChainedDataFrame<S, R, E, G, CDF, CEX>): ReadChainedDataFrame<S, R, E, G, CDF, CEX>;
    // (undocumented)
    unionByName<RCDF = unknown, RCEX = unknown>(right: ReadChainedDataFrame<S, R, E, G, RCDF, RCEX>, allowMissingColumns?: false): ReadChainedDataFrame<S, R, E, G, CDF & RCDF, CEX & RCEX>;
    // (undocumented)
    unionByName<RS, RCDF = unknown, RCEX = unknown>(right: ReadChainedDataFrame<RS, R, E, G, RCDF, RCEX>, allowMissingColumns: true): ReadChainedDataFrame<UnknownSchema, R, E, G, CDF & RCDF, CEX & RCEX>;
    // (undocumented)
    unpersist(blocking?: boolean): ReadChainedDataFrame<S, R, E, G, CDF & CacheCap<R>, CEX>;
    // (undocumented)
    withColumn<Name extends string, T extends ColumnType>(name: Name, build: TypedColumnFactory<S, T, E>): ReadChainedDataFrame<AddedColumnSchema<S, Name, T>, R, E, G, CDF, CEX>;
    // (undocumented)
    withColumn(name: string, e: DataFrameExpressionBuilder): ReadChainedDataFrame<UnknownSchema, R, E, G, CDF, CEX>;
    // (undocumented)
    withColumnRenamed<From extends ColumnName<S>, const To extends string>(oldName: From, newName: ValidRenameTarget<S, From, To>): ReadChainedDataFrame<RenamedSchema<S, From, To>, R, E, G, CDF, CEX>;
    // (undocumented)
    withColumnsRenamed<const M extends Record<string, string>>(mapping: ValidRenameMap<S, M>): ReadChainedDataFrame<RenamedColumnsSchema<S, M>, R, E, G, CDF, CEX>;
    // (undocumented)
    withWatermark(eventTimeCol: DataFrameExpressionBuilder, delay: string): ReadChainedDataFrame<S, R, E, G, CDF & EventTimeWatermarkCap<R, E>, CEX>;
    // (undocumented)
    write(this: ReadChainedDataFrame<S, R, E, G, Exclude<CDF, StreamingMark<R>>, CEX>): DataFrameWriterTF<R, E, G, WBatch, Exclude<CDF, StreamingMark<R>>, CEX, BatchWriterAlg<R>>;
    // (undocumented)
    writeStream(this: ReadChainedDataFrame<S, R, E, G, CDF & StreamingMark<R>, CEX>): DataFrameWriterTF<R, E, G, WStream, CDF & StreamingMark<R>, CEX, StreamWriterAlg<R>>;
}

// @public (undocumented)
export type ReadDFAny<R, E, G, CDF = unknown, CEX = unknown> = ReadDFPublic<R, E, G, CDF, CEX> | ReadDFPrivate<R, E, G, CDF, CEX>;

// @public (undocumented)
export type ReadDFPrivate<R, E, G, CDF = unknown, CEX = unknown> = {
    _getSession(): SparkSession;
    _getProgram(): DFProgram<R, E, G, CDF, CEX>;
};

// @public (undocumented)
export type ReadDFPublic<R, E, G, CDF = unknown, CEX = unknown> = {
    getSession(): SparkSession;
    getProgram(): DFProgram<R, E, G, CDF, CEX>;
};

// @public (undocumented)
export type ReaderOptions = Record<string, string>;

// @public (undocumented)
export type ReaderOptionValue = string | number | boolean | bigint;

// @public
export type Renamed<S extends SchemaShape, From extends keyof S, To extends string> = Prettify<Omit<S, From> & {
    [P in To]: S[From];
}>;

// @public (undocumented)
export type RenamedColumnsSchema<S, M extends Record<string, string>> = [S] extends [UnknownSchema] ? UnknownSchema : S extends SchemaShape ? {
    [K in keyof S as K extends keyof M ? Extract<M[K], string> : K]: S[K];
} : UnknownSchema;

// @public (undocumented)
export type RenamedSchema<S, From extends PropertyKey, To extends string> = [S] extends [UnknownSchema] ? UnknownSchema : S extends SchemaShape ? Renamed<S, Extract<From, keyof S>, To> : UnknownSchema;

// @public (undocumented)
export interface RepartitionCap<R> {
    // (undocumented)
    coalesce(plan: R, numPartitions: number): R;
    // (undocumented)
    repartition(plan: R, numPartitions: number, shuffle: boolean): R;
}

// @public (undocumented)
export type RetryConfig = {
    maxRetries?: number;
    initialBackoffMs?: number;
    maxBackoffMs?: number;
    backoffMultiplier?: number;
    onRetry?: (event: RetryEvent) => void;
};

// @public (undocumented)
export type RetryEvent = {
    attempt: number;
    delayMs: number;
    error: unknown;
};

// @public
export function round<T extends NumericValue | null, E>(col: TypedColumn<T, E>, scale?: number): NumericColumn<T, E>;

// @public
export type RowOf<S> = [S] extends [UnknownSchema] ? Record<string, unknown> : S extends SchemaShape ? {
    [K in keyof S]: S[K] extends ColumnType ? S[K] : unknown;
} : Record<string, unknown>;

// @public (undocumented)
export interface SamplingCap<R> {
    // (undocumented)
    sample(plan: R, lowerBound: number, upperBound: number, withReplacement?: boolean, seed?: number, deterministicOrder?: boolean): R;
}

// @public (undocumented)
export type SaveMode = "append" | "overwrite" | "ignore" | "error" | "errorifexists";

// @public
export type ScalarType = string | number | bigint | boolean;

// @public
export type Schema = Record<string, ColumnType>;

// @public
export function schema<D extends SchemaDef>(def: D): DeclaredSchema<D>;

// @public
export interface SchemaAggregation<Alias extends string = string, Out extends ColumnType = ColumnType> {
    // @internal
    readonly __out: {
        [P in Alias]: Out;
    };
}

// @public
export interface SchemaDef {
    // (undocumented)
    readonly [name: string]: FieldSpec;
}

// @public
export type SchemaShape = Record<string, ColumnType | AmbiguousColumn>;

// @public
export type Selected<S extends SchemaShape, K extends keyof S> = {
    [P in K]: S[P];
};

// @public (undocumented)
export type SelectedSchema<S, K extends PropertyKey> = [S] extends [UnknownSchema] ? UnknownSchema : S extends SchemaShape ? Selected<S, Extract<K, keyof S>> : UnknownSchema;

// @public (undocumented)
export interface SessionAlgebra {
    // (undocumented)
    readStream<R = unknown, E = unknown, G = unknown>(format: string, options?: Record<string, string>): ReadChainedDataFrame<UnknownSchema, R, E, G, StreamingReadCap<R> & StreamingMark<R>, unknown>;
    // (undocumented)
    sql<R = unknown, E = unknown, G = unknown>(query: string): ReadChainedDataFrame<UnknownSchema, R, E, G, SqlCap<R>, unknown>;
    // (undocumented)
    table<R = unknown, E = unknown, G = unknown>(name: string): ReadChainedDataFrame<UnknownSchema, R, E, G, SqlCap<R>, unknown>;
}

// @public (undocumented)
export type SessionConfigMap = Record<string, SessionConfigValue>;

// @public (undocumented)
export type SessionConfigValue = string | number | boolean;

// @public (undocumented)
export type SortDirection = "asc" | "desc";

// @public
export class SortKey<E> {
    // @internal
    constructor(run: TypedColumnExprThunk<E>, direction: SortDirection, nulls?: NullsOrder | undefined);
    build(EX: ExprAlg<E>): E;
    // (undocumented)
    readonly direction: SortDirection;
    // (undocumented)
    readonly nulls?: NullsOrder | undefined;
    toSortOrder(EX: ExprAlg<E>): SortOrder<E>;
}

// @public (undocumented)
export type SortKeyBuilder = <E>(EX: ExprAlg<E>) => {
    expr: E;
    direction: SortDirection;
    nulls?: NullsOrder;
};

// @public (undocumented)
export type SortOrder<E> = {
    expr: E;
    direction: SortDirection;
    nulls?: NullsOrder;
};

// @public (undocumented)
export const spark: SparkSession;

// @public
export class SparkConnectError extends Error {
    constructor(message: string, options: SparkConnectErrorOptions);
    // (undocumented)
    readonly cause?: unknown;
    // (undocumented)
    readonly code?: number;
    // (undocumented)
    readonly details?: string;
    // (undocumented)
    readonly errorClass?: string;
    // (undocumented)
    readonly errorId?: string;
    // (undocumented)
    readonly errorInfo?: SparkGrpcErrorInfo;
    // (undocumented)
    readonly metadata?: grpc.Metadata;
    // (undocumented)
    readonly operation: string;
    // (undocumented)
    readonly remoteDetails?: SparkErrorDetails;
}

// @public (undocumented)
export type SparkConnectErrorOptions = {
    operation: string;
    code?: number;
    details?: string;
    metadata?: grpc.Metadata;
    errorId?: string;
    errorClass?: string;
    errorInfo?: SparkGrpcErrorInfo;
    remoteDetails?: SparkErrorDetails;
    cause?: unknown;
};

// @public (undocumented)
export type SparkConnectionConfig = {
    address?: string;
    auth?: AuthConfig;
    allowInsecureAuth?: boolean;
    rpcTimeoutMs?: number;
    signal?: AbortSignal;
    grpcMaxReceiveMessageBytes?: number;
    grpcMaxSendMessageBytes?: number;
    tls?: TLSConfig;
    retry?: RetryConfig;
    logger?: SparkLogger;
    metrics?: SparkMetricObserver;
    sessionConfig?: SessionConfigMap;
};

// @public
export type SparkErrorDetails = Record<string, unknown>;

// @public
export type SparkGrpcErrorInfo = {
    reason?: string;
    domain?: string;
    metadata: Record<string, string>;
};

// @public
export type SparkLogger = (event: Readonly<SparkTelemetryEvent>) => void;

// @public (undocumented)
export type SparkMetric = {
    name: string;
    kind: "counter" | "histogram";
    value: number;
    unit: "count" | "milliseconds";
    timestamp: string;
    attributes: Record<string, unknown>;
};

// @public
export type SparkMetricObserver = (metric: Readonly<SparkMetric>) => void | PromiseLike<void>;

// @public
export class SparkRuntimeConfig {
    // @internal
    constructor(session: SparkSession);
    // (undocumented)
    get(key: string, defaultValue?: SessionConfigValue): Promise<string>;
    // (undocumented)
    getAll(prefix?: string): Promise<Record<string, string>>;
    // (undocumented)
    getOption(key: string): Promise<string | undefined>;
    // (undocumented)
    isModifiable(key: string): Promise<boolean>;
    // (undocumented)
    set(key: string, value: SessionConfigValue): Promise<void>;
    // (undocumented)
    unset(key: string): Promise<void>;
}

// @public (undocumented)
export class SparkSession implements SessionAlgebra {
    constructor(sessionId?: string, opts?: {
        userContext?: Partial<SparkUserContext>;
        connectionConfig?: SparkConnectionConfig;
    });
    // (undocumented)
    static builder(): SparkSessionBuilder;
    close(): Promise<void>;
    get conf(): SparkRuntimeConfig;
    // @internal
    ensureRemoteConfigApplied(): Promise<void>;
    // (undocumented)
    getConnectionConfig(): SparkConnectionConfig;
    // @internal
    getServerSideSessionId(): string | undefined;
    // (undocumented)
    getSessionConfig(): SessionConfigMap;
    // (undocumented)
    getSessionId(): string;
    // (undocumented)
    getUserContext(): SparkUserContext;
    // @internal
    markRemoteTouched(): void;
    // @internal
    observeRemoteError(error: unknown): void;
    // @internal
    observeServerSideSessionId(response: unknown): void;
    // (undocumented)
    get read(): DataFrameReaderTF;
    // (undocumented)
    readStream<R, E, G>(format: string, options?: Record<string, string>): ReadChainedDataFrame<UnknownSchema, R, E, G, StreamingReadCap<R> & StreamingMark<R>, unknown>;
    // @internal
    runConfigOperation(operation: Record<string, unknown>): Promise<Record<string, unknown>>;
    // (undocumented)
    setConnectionConfig(config: SparkConnectionConfig): void;
    // (undocumented)
    setUserContext(context: Partial<SparkUserContext>): void;
    // (undocumented)
    sql<R = unknown, E = unknown, G = unknown>(query: string): ReadChainedDataFrame<UnknownSchema, R, E, G, SqlCap<R>, unknown>;
    stop(): Promise<void>;
    // (undocumented)
    table<R = unknown, E = unknown, G = unknown>(name: string): ReadChainedDataFrame<UnknownSchema, R, E, G, SqlCap<R>, unknown>;
    // @internal
    updateLocalSessionConfig(key: string, value: SessionConfigValue | undefined): void;
    // (undocumented)
    writeStream<R = unknown, E = unknown, G = unknown, CDF = unknown, CEX = unknown>(df: ReadChainedDataFrame<UnknownSchema, R, E, G, CDF & StreamingMark<R>, CEX>): DataFrameWriterTF<R, E, G, WStream, CDF & StreamingMark<R>, CEX, StreamWriterAlg<R>>;
}

// @public (undocumented)
export class SparkSessionBuilder {
    allowInsecureAuth(allow?: boolean): this;
    // (undocumented)
    config(key: string, value: SessionConfigValue): this;
    // (undocumented)
    configs(configs: SessionConfigMap): this;
    // (undocumented)
    enableTLS(tls: TLSConfig): this;
    getOrCreate(): SparkSession;
    // (undocumented)
    user(context: Partial<SparkUserContext>): this;
    withAbortSignal(signal: AbortSignal): this;
    // (undocumented)
    withAuth(auth: AuthConfig): this;
    // (undocumented)
    withAuthAndTLS(auth: AuthConfig, tls: TLSConfig): this;
    withLogger(logger: SparkLogger): this;
    withMetrics(observer: SparkMetricObserver): this;
    // (undocumented)
    withRetry(retry: RetryConfig): this;
    withRpcTimeout(timeoutMs: number): this;
}

// @public (undocumented)
export type SparkTelemetryEvent = {
    name: string;
    level: "debug" | "info" | "warn" | "error";
    timestamp: string;
    attributes: Record<string, unknown>;
};

// @public (undocumented)
export type SparkUserContext = {
    user_id: string;
    user_name?: string;
};

// @public (undocumented)
export function split(input: EBuilder, delimiter: string | EBuilder): EBuilder;

// @public (undocumented)
export interface SqlCap<R> {
    // (undocumented)
    sql(query: string): R;
}

// @public (undocumented)
export type StatisticsSchema<S, K extends PropertyKey> = [S] extends [UnknownSchema] ? UnknownSchema : S extends SchemaShape ? {
    summary: string;
} & {
    [P in Extract<K, keyof S>]: string | null;
} : UnknownSchema;

// @public (undocumented)
export interface StreamFormatCap {
    // (undocumented)
    format(w: WStream, fmt: StreamWriterFormat): WStream;
}

// @public (undocumented)
export type StreamingMark<R> = {
    [STREAMING_BRAND]: (r: R) => R;
};

// @public (undocumented)
export interface StreamingQueryHandle {
    // (undocumented)
    awaitTermination(): Promise<void>;
    // (undocumented)
    awaitTermination(timeoutMs: number): Promise<boolean>;
    // (undocumented)
    name: string;
    // @internal
    readonly serverSideSessionId?: string;
    // (undocumented)
    stop(): Promise<unknown>;
}

// @public (undocumented)
export interface StreamingReadCap<R> {
    // (undocumented)
    readStream(format: string, options?: Record<string, string>): R;
}

// @public (undocumented)
export interface StreamLiftCap<R> {
    // (undocumented)
    writeStream(df: R): WStream;
}

// @public (undocumented)
export type StreamTriggerInput = {
    processingTime: string;
} | {
    once: true;
} | {
    availableNow: true;
} | {
    continuous: string;
} | {
    kind: "ProcessingTime";
    intervalMs: number;
} | {
    kind: "Once";
} | {
    kind: "Continuous";
    checkpointIntervalMs: number;
};

// @public
export type StreamWProgram<R, E, G, CDF = unknown, CEX = unknown> = WProgram<R, E, G, WStream, CDF, CEX, StreamWriterAlg<R>>;

// @public (undocumented)
export type StreamWriterAlg<R> = WriteCore<R, WStream> & StreamLiftCap<R> & // ← writeStream(df)
StreamFormatCap & // ← AQUÍ
OutputModeCap<WStream> & TriggerCap<WStream> & CheckpointCap<WStream> & QueryNameCap<WStream>;

// @public (undocumented)
export type StreamWriterFormat = "console" | "kafka" | "memory";

// @public (undocumented)
export function struct(...fields: EBuilder[]): EBuilder;

// @public (undocumented)
export interface StructSpec<Fields extends SchemaDef = SchemaDef> {
    // (undocumented)
    readonly fields: Fields;
    // (undocumented)
    readonly kind: "struct";
    // (undocumented)
    readonly nullable?: boolean;
}

// @public (undocumented)
export function structType<const Fields extends SchemaDef, const N extends boolean = false>(fields: Fields, nullable?: N): StructSpec<Fields> & NullableFlag<N>;

// @public (undocumented)
export interface SummaryCap<R, E> {
    // (undocumented)
    summary(plan: R, metrics: E[], columns: E[]): R;
}

// @public (undocumented)
export type SumResult<S extends SchemaShape, K extends NumericColumnKey<S>> = NumericResult<S, K> extends bigint ? bigint : number | bigint;

// @public (undocumented)
export type TLSConfig = {
    keyStorePath?: string;
    keyStorePassword?: string;
    trustStorePath?: string;
    trustStorePassword?: string;
    certChainPath?: string;
    privateKeyPath?: string;
    serverNameOverride?: string;
};

// @public (undocumented)
export function to_json(expr: EBuilder): EBuilder;

// @public
export const TOKEN_TO_DDL: Record<TypeToken, string>;

// @public
export interface TokenToTs {
    // (undocumented)
    boolean: boolean;
    // (undocumented)
    date: string;
    // (undocumented)
    double: number;
    // (undocumented)
    float: number;
    // (undocumented)
    int: number;
    // (undocumented)
    long: bigint;
    // (undocumented)
    string: string;
    // (undocumented)
    timestamp: string;
}

// @public (undocumented)
export type Trigger = {
    kind: "ProcessingTime";
    intervalMs: number;
} | {
    kind: "Once";
} | {
    kind: "Continuous";
    checkpointIntervalMs: number;
};

// @public (undocumented)
export interface TriggerCap<W> {
    // (undocumented)
    trigger(w: W, t: Trigger): W;
}

// @public (undocumented)
export type TypedAggBuilder<S, E, A extends readonly TypedAggregation<string, ColumnType, E>[]> = S extends SchemaShape ? (factory: AggFactory<S, E>) => readonly [...A] & UniqueAggregationTuple<A> : never;

// @public
export class TypedAggregation<Alias extends string, T extends ColumnType, E> implements SchemaAggregation<Alias, T> {
    // @internal
    constructor(alias: Alias, run: AggregationExprThunk<E>);
    // @internal
    readonly __out: {
        [P in Alias]: T;
    };
    // (undocumented)
    readonly alias: Alias;
    build(EX: ExprAlg<E>): E;
}

// @public
export interface TypedCaseBranch<T extends ScalarType, E> {
    // (undocumented)
    readonly cond: Condition<E>;
    // (undocumented)
    readonly value: T | TypedColumn<T, E>;
}

// @public
export class TypedCaseChain<T extends ScalarType, E> {
    // @internal
    constructor(branches: ReadonlyArray<TypedCaseBranch<T, E>>);
    otherwise(value: T | TypedColumn<T, E> | null): TypedColumn<T | null, E>;
    when(cond: Condition<E>, value: T | TypedColumn<T, E>): TypedCaseChain<T, E>;
}

// @public
export class TypedColumn<T extends ColumnType, E> {
    // @internal
    constructor(run: TypedColumnExprThunk<E>);
    as(name: string): TypedColumn<T, E>;
    asc(nulls?: NullsOrder): SortKey<E>;
    build(EX: ExprAlg<E>): E;
    coalesce(fallback: Extract<NonNull<T>, ScalarType>): TypedColumn<NonNull<T>, E>;
    // (undocumented)
    coalesce<U extends NonNull<T> | null>(fallback: TypedColumn<U, E>): TypedColumn<Coalesced<T, U>, E>;
    desc(nulls?: NullsOrder): SortKey<E>;
    eq(other: Extract<NonNull<T>, ScalarType> | TypedColumn<NonNull<T>, E> | TypedColumn<NonNull<T> | null, E>): Condition<E>;
    gt(other: Extract<NonNull<T>, ScalarType> | TypedColumn<NonNull<T>, E> | TypedColumn<NonNull<T> | null, E>): Condition<E>;
    gte(other: Extract<NonNull<T>, ScalarType> | TypedColumn<NonNull<T>, E> | TypedColumn<NonNull<T> | null, E>): Condition<E>;
    isNotNull(): Condition<E>;
    isNull(): Condition<E>;
    lt(other: Extract<NonNull<T>, ScalarType> | TypedColumn<NonNull<T>, E> | TypedColumn<NonNull<T> | null, E>): Condition<E>;
    lte(other: Extract<NonNull<T>, ScalarType> | TypedColumn<NonNull<T>, E> | TypedColumn<NonNull<T> | null, E>): Condition<E>;
    // (undocumented)
    protected readonly run: TypedColumnExprThunk<E>;
}

// @public
export type TypedColumnExprThunk<E> = (EX: ExprAlg<E>) => E;

// @public (undocumented)
export type TypedColumnFactory<S, T extends ColumnType, E> = S extends SchemaShape ? (columns: Columns<S, E>) => TypedColumn<T, E> : never;

// @public (undocumented)
export type TypedJoinPredicate<S, RS extends SchemaShape, E> = S extends SchemaShape ? (left: Columns<S, E>, right: Columns<RS, E>) => Condition<E> : never;

// @public (undocumented)
export type TypedOrderFactory<S, E> = S extends SchemaShape ? (columns: Columns<S, E>) => SortKey<E> | TypedColumn<ColumnType, E> : never;

// @public (undocumented)
export type TypedPredicate<S, E> = S extends SchemaShape ? (columns: Columns<S, E>) => Condition<E> : never;

// @public
export function typedWhen<V extends ScalarType, E>(cond: Condition<E>, value: V | TypedColumn<Widen<V>, E>): TypedCaseChain<Widen<V>, E>;

// @public
export type TypeToken = "int" | "long" | "double" | "float" | "string" | "boolean" | "date" | "timestamp";

// @public
export type UnionToIntersection<U> = (U extends unknown ? (k: U) => void : never) extends (k: infer I) => void ? I : never;

// @public (undocumented)
export type UniqueAggregationTuple<A extends readonly {
    readonly alias: string;
}[], Seen extends string = never> = A extends readonly [infer Head, ...infer Tail] ? Head extends {
    readonly alias: infer Alias extends string;
} ? Alias extends Seen ? never : Tail extends readonly {
    readonly alias: string;
}[] ? UniqueAggregationTuple<Tail, Seen | Alias> : unknown : unknown : unknown;

// @public
export type UnknownSchema = {
    readonly __unknownSchema: unique symbol;
};

// @public
export function upper<T extends string | null, E>(col: TypedColumn<T, E>): TypedColumn<T, E>;

// @public
export type UsableColumnKey<S> = {
    [K in keyof S]-?: S[K] extends ColumnType ? K : never;
}[keyof S] & string;

// @public
export type UsableColumnType<S, K extends keyof S> = Extract<S[K], ColumnType>;

// @public (undocumented)
export type ValidRenameMap<S, M extends Record<string, string>> = DuplicateRenameTargets<M> extends never ? [S] extends [UnknownSchema] ? M : Exclude<keyof M, keyof Extract<S, SchemaShape>> extends never ? Extract<M[keyof M], Exclude<keyof Extract<S, SchemaShape>, keyof M>> extends never ? M : never : never : never;

// @public (undocumented)
export type ValidRenameTarget<S, From extends PropertyKey, To extends string> = [
S
] extends [UnknownSchema] ? To : To extends Extract<Exclude<keyof Extract<S, SchemaShape>, From>, string> ? never : To;

// @public (undocumented)
export interface ViewsCap<W> {
    // (undocumented)
    createOrReplaceTempView(w: W, viewName: string): W;
    // (undocumented)
    createTempView(w: W, name: string): W;
    // (undocumented)
    registerView?(w: W, name: string, replace: boolean): W;
}

// @public (undocumented)
export type WBatch = {} & WBatchBrand;

// @public (undocumented)
export interface WBatchBrand {
    // (undocumented)
    __wflavor?: "batch";
}

// @public (undocumented)
export function when(cond: EBuilder, val: EBuilder | string | number | boolean): ExpressionCaseChain;

// @public
export type Widen<V> = V extends string ? string : V extends number ? number : V extends bigint ? bigint : V extends boolean ? boolean : V;

// @public (undocumented)
const Window_2: {
    partitionBy: (...keys: (string | EBuilder)[]) => {
        orderBy: (...ords: Array<string | EBuilder | SortKeyBuilder>) => {
            rowsBetween: (start: FrameBoundary, end: FrameBoundary) => <E>(EX: ExprAlg<E>) => WindowSpec<E>;
        };
    };
};
export { Window_2 as Window }

// @public (undocumented)
export type WindowFrameBoundary = {
    type: "UnboundedPreceding";
} | {
    type: "UnboundedFollowing";
} | {
    type: "CurrentRow";
} | {
    type: "ValuePreceding";
    value: number;
} | {
    type: "ValueFollowing";
    value: number;
};

// @public (undocumented)
export type WindowSpec<E> = {
    partitionBy: E[];
    orderBy: Array<{
        input: E;
        direction: SortDirection;
        nulls?: NullsOrder;
    }>;
    frame?: {
        type: FrameType;
        start: WindowFrameBoundary;
        end: WindowFrameBoundary;
    };
};

// @public
export type WithColumn<S extends SchemaShape, Name extends string, T extends ColumnType> = Prettify<Omit<S, Name> & {
    [P in Name]: T;
}>;

// @public
export type WProgram<R, E, G, // tipos del mundo DF
W, // nodo del writer (WBatch | WStream)
CDF = unknown, CEX = unknown, WRALG = unknown> = (WR: WRALG, DF: DFAlg<R, E, G, CDF>, EX: ExprAlg<E> & CEX) => W;

// @public
export interface WriteCore<R, W> {
    // (undocumented)
    format(w: W, fmt: string): W;
    fromChild(child: R): W;
    // (undocumented)
    option(w: W, k: string, v: string): W;
    // (undocumented)
    options(w: W, opts: Record<string, string>): W;
    // (undocumented)
    partitionBy(w: W, ...cols: string[]): W;
    // (undocumented)
    targetPath(w: W, path: string): W;
    // (undocumented)
    targetTable(w: W, table: string): W;
}

// @public (undocumented)
export type WriterBackendImplementation<R, E, G, W, CDF, CEX, WRALG> = {
    DF: DFAlg<R, E, G, CDF>;
    EX: ExprAlg<E> & CEX;
    WR: WRALG;
    EXE: DFWritingExec<W>;
};

// @public (undocumented)
export type WriterOptionValue = string | number | boolean | bigint;

// @public (undocumented)
export type WStream = {} & WStreamBrand;

// @public (undocumented)
export interface WStreamBrand {
    // (undocumented)
    __wflavor?: "stream";
}

// (No @packageDocumentation comment for this package)

```
