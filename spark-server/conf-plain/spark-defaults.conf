# Plain gRPC profile for trusted local debugging only.
# The default Docker profile is TLS; see docker-compose.plain.yml.
spark.connect.grpc.ssl.enabled=false
spark.connect.grpc.binding.port=15002
spark.connect.grpc.ui.enabled=true
spark.sql.catalogImplementation=in-memory

spark.sql.catalog.spark_catalog.type=hadoop
spark.sql.catalog.spark_catalog.warehouse=/tmp/warehouse
spark.sql.warehouse.dir=/tmp/warehouse
