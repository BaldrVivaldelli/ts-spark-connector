# Development TLS profile. The keystore and password are public test fixtures;
# use docker-compose.plain.yml only for isolated plaintext debugging.
spark.ssl.enabled=true
spark.ssl.keyStore=/opt/certs/keystore.p12
spark.ssl.keyStorePassword=password
spark.ssl.keyStoreType=PKCS12

spark.connect.grpc.ssl.enabled=true
spark.connect.grpc.binding.port=15002
spark.connect.grpc.ui.enabled=true
spark.sql.catalogImplementation=in-memory

# Optional TLS handshake diagnostics.
# spark.ssl.debug=true

# Local catalog/warehouse settings for disposable development data.
spark.sql.catalog.spark_catalog.type=hadoop
spark.sql.catalog.spark_catalog.warehouse=/tmp/warehouse

# Compatibility fallback used by Spark paths that read the legacy setting.
spark.sql.warehouse.dir=/tmp/warehouse
